# File Sharing Security Baseline

Keep buckets private. Never ship storage credentials in Flutter. Use short-lived signed URLs only after authorization. Enforce quotas server-side. Verify stored size before finalization. Normalize filenames and generate opaque object keys. Treat MIME declarations as untrusted. Add malware scanning before production availability. Never log file contents, signed URLs, storage credentials, or private attachment data.
