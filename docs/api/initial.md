# Initial API surface

- `GET /health`
- `POST /api/v1/auth/google`
- `GET /api/v1/files/quota`

Planned versioned resources: `/users`, `/contacts`, `/conversations`, `/messages`, `/groups`, `/files`, `/feel-it`, `/notifications`, `/sessions`, `/reports`.
